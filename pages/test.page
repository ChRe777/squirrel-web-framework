---

--> TRANSFORM

import Foo from '../components/foo.squirrel';
import Bar from '../components/bar.squirrel';

--> TO -->

const mapping = {}

const Foo = importDynamic('../components/foo.squirrel');
mapping["Foo"] = Foo;

const Bar = importDynamic('../components/bar.squirrel');
mapping["Bar"] = Bar;

const id = "id123";

---

const html = compile(mapping, `
<div id=${id}>
    <Bar/>
    <Foo/>
</div>
`);

return html;

---

transform(tagName,
