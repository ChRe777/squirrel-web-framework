---
// Fetch data
const response = await fetch('https://jsonplaceholder.typicode.com/users');
const users = await response.json();
//
export const id = "users";
export const is_dyn = true;
---
<h3>Users</h3>
<ul>
    ${users.map((user)=> html`<li>${user.name}</li>`)}
</ul>
